import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    height: 42,
    paddingHorizontal: 5,
    marginTop: 20,
    borderRadius: 4,
    alignItems: 'center',
    justifyContent: 'space-evenly',
  },

  textButton: {
    fontWeight: 'bold',
    fontSize: 16,
  },

  loaderSection: {
    flexDirection: 'row',
  },
});
