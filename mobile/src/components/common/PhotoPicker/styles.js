import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    wrapper: {
        paddingHorizontal: 30
    },

    pickerOptions: {
        flexDirection: 'row',
        paddingTop: 30,
        alignItems: 'center'
    },

    text: {
        fontSize: 20,
        paddingLeft: 20
    }
});
